The CDS View is exposed as an OData service using the annotation
@OData.publish: true.

When activated in SAP S/4HANA ABAP environment, the system automatically
creates:
- OData service
- Metadata document
- Service endpoint
